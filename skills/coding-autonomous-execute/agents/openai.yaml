interface:
  display_name: "CODING | Autonomous Execute"
  short_description: "Build ready features serially to evaluator PASS."
  default_prompt: "Use $coding-autonomous-execute. Select the lowest-priority ready feature, complete its full retained proof, repair, and fresh-evaluator lifecycle serially through PASS, then select the next ready feature. Stop when none remain."
