interface:
  display_name: "CODING | Fix Issue"
  short_description: "Apply minimal corrective changes with regression verification."
  default_prompt: "Use $coding-fix-issue to fix this issue with the smallest effective change and regression verification."
policy:
  allow_implicit_invocation: true
