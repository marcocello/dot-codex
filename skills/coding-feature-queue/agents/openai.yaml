interface:
  display_name: "CODING | Feature Queue"
  short_description: "Maintain the small feature queue."
  default_prompt: "Use $coding-feature-queue. Keep draft, ready, revalidate, blocked, and done distinct; default builds select ready while explicit proof-only revalidation handles revalidate."
policy:
  allow_implicit_invocation: true
