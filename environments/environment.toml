# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "meshify-backend"

[setup]
script = '''
REPO_NAME=$(basename "$PWD")

for base in /Users/marcocello/software/marcocello /Users/marcocello/software/scaleuplabs; do
  SRC="$base/$REPO_NAME/.env"
  if [ -f "$SRC" ]; then
    cp "$SRC" .
    echo "Copied from $SRC"
    break
  fi
done

python3 -m venv .venv
. .venv/bin/activate
pip install -r app/requirements.txt
'''
