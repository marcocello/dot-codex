# Single knowledge connection skill

User requests one knowledge-connect skill for configuration and provider operations, clear missing prerequisites/capability limits, ClickUp token generation via Settings → ClickUp API → API tokens, and permission to paste tokens in chat.

Move canonical knowledge_profile.py and configuration references into knowledge-connect. Remove knowledge-config SKILL.md and UI metadata from active inventory via skill_inventory.py. Retain old resolver/ClickUp CLI paths as compatibility launchers; no duplicate runtime logic. Current version-1 profiles, credential binding, temporary/persisted transport, none/Markdown, account isolation and API behavior remain unchanged. SETUP_REQUIRED now identifies knowledge-connect. Update current consumers/AGENTS/README/routing and UI metadata; historical feature evidence remains unchanged.

One entrypoint owns capability assessment, setup, profiles, token guidance, identity verification, operations and recovery. Detailed setup is a linked reference; do not merge everything into a huge entrypoint. Explain which prerequisite is missing (plugin unavailable/disconnected, token absent/invalid, ambiguous workspace/target, permission or unsupported API operation) and next applicable action. No blanket claim API solves all MCP limitations. Preserve the newly present ClickUp Docs/page implementation from other completed work. Explain actual remaining limitations (e.g. Notion body/block API, unsupported comments/attachments/deletion) before token setup; do not restore obsolete claims that ClickUp Docs is unsupported.

Token guidance uses the user's ClickUp path. Accept voluntary pasted token values and offer chat as a supported input option alongside an environment reference or private file. Briefly explain chat input remains in conversation history; don't demand rotation solely because user chose chat. Do not echo/repeat the value, put it in tracked configuration/repo, CLI arguments or error output. Chat-token provisioning is agent guidance, not a new input frontend: use an available secret-input/secure-storage mechanism or a private file outside the repo through applicable authorized filesystem tools without embedding secrets in shell commands. If no suitable mechanism is available, state that limitation and offer the existing private terminal path; do not claim storage occurred or refuse merely because chat input was used. Saving credentials requires a chosen account/profile and user authorization; within an explicitly requested setup, a supplied token is authorized for that setup, not unrelated use. Profile TOML retains only references. No live token provided/created in this task.

Independent proof: public canonical and legacy CLI tests for connection creation/readback, credentials, temporary override and updated missing-setup owner; API regressions preserved. Independent semantic inspection for unified routing, clear prerequisites/limits, exact ClickUp path, chat token acceptance and no echo/repository storage. No live SaaS claim.
