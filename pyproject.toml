[tool.ruff]
line-length = 100
exclude = [
    ".tmp",
    "agent-scripts",
    "claude-code-config",
    "get-shit-done",
    "plugins/cache",
]

[tool.mypy]
python_version = "3.13"
exclude = [
    "agent-scripts/",
    "claude-code-config/",
    "get-shit-done/",
    "plugins/cache/",
]
