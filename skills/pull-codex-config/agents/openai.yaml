interface:
  display_name: "Pull Codex Config"
  short_description: "Safely update this Codex config repo"
  default_prompt: "Use $pull-codex-config to fast-forward my local dot-codex configuration checkout."
