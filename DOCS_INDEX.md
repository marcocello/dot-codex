# Docs Index

Use this index to locate domain and implementation guidance before coding.

## Global guidance
- Prefer per-repo `docs/INDEX.md` or `docs/README.md` when available.
- If neither exists, scan the repo `docs/` directory for the most relevant files.
- Only add cross-repo references here when they apply broadly across multiple projects.
