interface:
  display_name: "CODING | Repair"
  short_description: "Latest-failure-first narrow repair."
  default_prompt: "Use $coding-repair. Read latest result and relevant error, classify owner, reproduce narrowly, repair minimally, rerun narrow and full proof."
policy:
  allow_implicit_invocation: true
