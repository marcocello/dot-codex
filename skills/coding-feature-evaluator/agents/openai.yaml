interface:
  display_name: "CODING | Done Evaluator"
  short_description: "Read-only semantic done judge."
  default_prompt: "Use $coding-feature-evaluator. Stay read-only. Judge user-intent alignment, architecture quality, implementation, realistic proof, false-green risk, gaps, and gate. Return PASS, FAIL, or NEED_INPUT."
policy:
  allow_implicit_invocation: true
