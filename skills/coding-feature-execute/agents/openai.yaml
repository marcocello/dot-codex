interface:
  display_name: "CODING | Feature Execute"
  short_description: "Implement, prove, repair, auto-evaluate one feature."
  default_prompt: "Use $coding-feature-execute for one ready feature. Manage proof, repair, gate, fresh evaluator subagent, and queue result."
policy:
  allow_implicit_invocation: true
