interface:
  display_name: "CODING | Prepare Environment"
  short_description: "Prepare envs, whitelist gitignore, and run tasks."
  default_prompt: "Use $coding-prepare-environment to prepare only the local environment and narrow readiness checks needed for this work, preserving existing setup and adding ignore rules or run tasks when needed."
policy:
  allow_implicit_invocation: true
