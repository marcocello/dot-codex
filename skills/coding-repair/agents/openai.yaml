interface:
  display_name: "CODING | Repair"
  short_description: "Latest-failure-first narrow repair."
  default_prompt: "Use $coding-repair. Read the latest proof failure or review finding, inherit active feature assurance when present, reproduce narrowly, repair minimally, rerun the full feature proof, and return for fresh final review."
policy:
  allow_implicit_invocation: true
