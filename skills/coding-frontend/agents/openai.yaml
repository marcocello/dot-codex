interface:
  display_name: "CODING | Frontend"
  short_description: "Own React/Next.js construction before platform scaffolds."
  default_prompt: "Use $coding-frontend to construct or change this React or Next.js interface, preserving repository conventions and selecting Sites only when explicitly requested or already established."
policy:
  allow_implicit_invocation: true
