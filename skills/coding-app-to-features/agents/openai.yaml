interface:
  display_name: "CODING | App To Features"
  short_description: "Shape app context and decided vertical features."
  default_prompt: "Use $coding-app-to-features. Ask material questions, challenge the app idea, state decisions, then create the smallest decision-ready vertical feature queue without approval gates."
policy:
  allow_implicit_invocation: true
