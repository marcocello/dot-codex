# Completion

## Proof
- PASS: captured attempt `20260727T201255367637Z`.
- The real gate CLI accepted a normal target repository without `AGENTS.md`.
- The harness profile reported its own missing-global-kernel failure.
- The active configuration read back `sites_enabled=true`.
- The global routing policy, frontend skill boundary, and removed nested `.codex` symlink passed their focused regressions.

## Gate
- PASS: `scripts/gate --root "$PWD"`.
- Common, Python, harness lint, unit tests, and diff hygiene passed.

## Evaluator
- PASS.
- Intent remained aligned with keeping Sites enabled while routing generic React/Next construction to `coding-frontend`.
- The evaluator accepted the real gate subprocess proof, active configuration and instruction read-back, retained red/green attempts, and the declared probabilistic model-compliance limitation.

## Repairs
- Moved the `AGENTS.md` requirement from the common gate to the dot-codex harness profile.
- Kept Sites enabled and added pre-scaffold ownership rules instead of editing the bundled Sites plugin.
- Removed the ignored self-referential `.codex` symlink.
