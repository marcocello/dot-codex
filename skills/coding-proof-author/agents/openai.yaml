interface:
  display_name: "CODING | Proof Author"
  short_description: "Design realistic proof of the user lifecycle."
  default_prompt: "Use $coding-proof-author to design executable evidence across the real activation, relevant existing state, visible read-back, affected consumers, and named runtime target."
policy:
  allow_implicit_invocation: true
