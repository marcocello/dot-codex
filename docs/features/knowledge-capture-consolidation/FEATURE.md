# Knowledge Capture Consolidation

## Accepted outcome

Replace `capture-note`, `second-brain-capture-raw-input`, `second-brain-external-sweep`, and `zotero-capture-source` with one repository-owned `knowledge-capture` skill. The user approved a single capture skill and explicitly requested that raw input and external sweeps be together. Preserve `harness-capture-interactions` as a separate, explicitly invoked interaction-history tool.

## Consumption target and boundary

The target is the local dot-codex skill inventory and the instructions/resources loaded from the installed skill directory. This is an instruction consolidation, not an external capture execution. Verify inventory discovery, readable mode routing, preserved helper behavior, and independent scenario walkthroughs against the actual candidate instructions. Do not claim a live Notion/Zotero write, probabilistic model evaluation, or refreshed app discovery from these checks.

## Journeys and routing

One entrypoint resolves supplied material or requested source scope, then the destination. An explicit destination wins; established context may resolve an omitted destination. Ask one concise question only when multiple destinations remain plausible. A bare note must not trigger source gathering. A URL alone must not silently authorize a Zotero archive or a Notion record. No destination automatically writes to another destination.

Load only the relevant mode instructions:

1. **Markdown notes:** preserve the existing collection discovery, filename/frontmatter/organization conventions, append-versus-create behavior, unrelated content, factual uncertainty, local capture dates, and concise private receipt. Read the saved note back before claiming success. Do not introduce a new taxonomy or restructure existing notes.
2. **Second Brain:** use one common classify/deduplicate/write/verify workflow for both supplied material and explicitly requested external-source gathering. Gathering first resolves source scope and time window, reads only available requested sources, and reports inaccessible sources. Source systems remain native. Use `docs/harness/secondbrain.md` and its three active tables, existing properties and conservative values. Concrete actions go to Tasks; commercial evidence goes to Deals; proposals and insights go to Ideas. No Inbox, bulk transcripts, invented dates/owners/relationships, or silent deal advancement from indirect evidence. Inspect relevant existing records before create, comparing source links and the actual action/opportunity/idea; do not conflate unrelated items sharing a title. Update a clear match only through an available supported write path; ask or propose when ambiguous. If reads are incomplete, do not claim duplicate absence or blindly create. Verify actual writes through read-back. Without configured read/write capability, return exact proposed table, fields, values and existing-row identity when known, clearly distinguishing proposals from writes.
3. **Research sources:** retain Zotero companion readiness and target selection, canonical-source deduplication, transcript provenance and uncertainty, article access limits, grounded highlights, separate exact child-note names, precomputed metadata/notes, one connector session, tag preservation, expired-session stop without re-import, and parent/child read-back. Preserve both existing Python helpers byte-for-byte and the note-shapes reference, relocating them under the new skill and updating instruction links. Unsupported updates to existing items produce prepared notes and a clear blocker, never duplicate imports.

## Inventory and compatibility

Use `skill_inventory.py` for four removals and one owned addition. The current baseline has 47 declarations; the expected result is 44 with every other declaration unchanged. Retire all four old skill directories after preserving needed content. Update live harness routing and UI metadata to `knowledge-capture`; historical specifications, audits and proof remain history. Provide three substantive mode references rather than retaining the four old skills behind a fifth wrapper.

Keep the separate Notion API skill unchanged. Its current placeholder IDs and limited CLI do not establish usable access: the new capture instructions must verify configuration and operation support, prefer an available connector, and otherwise provide manual proposals. Do not repair or expand the adapter in this feature. Keep `harness-capture-interactions`, all unrelated skills, plugins, prior consolidated behavior, and unrelated dirty work unchanged.

## Verification and non-goals

Independent proof must cover destination ambiguity, Markdown append and privacy, supplied-note capture without sweep, a scoped sweep entering the same Second Brain process, deduplication/incomplete reads/manual fallback, conservative deal updates, and Zotero missing source/session-expiry/read-back behavior. Exercise the relocated RIS helper offline and verify helper/reference preservation and inventory discovery. No external writes or live credentials are needed for this target. Select affected inventory/metadata/reference regression checks and the repository gate separately. Obtain a fresh distinct read-only final review after passing official proof and regression evidence.

No further portfolio removals, API backend implementation, platform/plugin changes, commits, pushes, or deployments are included.
