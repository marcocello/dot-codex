interface:
  display_name: "CODING | Commit"
  short_description: "Create one or more coherent local commits."
  default_prompt: "Use $coding-commit to inspect local changes, stage selected files, and create one or more local Conventional Commits. Never push."
policy:
  allow_implicit_invocation: true
