interface:
  display_name: "CODING | Commit"
  short_description: "Create one or more coherent local commits."
  default_prompt: "Use $coding-commit to inspect repository changes, including changes from other tasks, and create one or more coherent local Conventional Commits. Never push."
policy:
  allow_implicit_invocation: true
