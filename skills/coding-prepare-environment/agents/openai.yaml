interface:
  display_name: "CODING | Prepare Environment"
  short_description: "Prepare repo environments before implementation work."
  default_prompt: >-
    Use $coding-prepare-environment to inspect this repository and prepare the local
    environment before running tests or implementation.
policy:
  allow_implicit_invocation: true
